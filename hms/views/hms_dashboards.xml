<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="hms_dashboards_action" model="ir.actions.client">
        <field name="name">Hospital Management — Dashboard</field>
        <field name="tag">hms.hms_dashboards_action</field>
    </record>
            <record id="action_open_my_activities" model="ir.actions.server">
        <field name="name">My Activities</field>
        <field name="model_id" ref="mail.model_mail_activity"/>
        <field name="state">code</field>
        <field name="code">action = env['hms.dashboard'].sudo().action_open_my_activities()</field>
    </record>

</odoo>